import { formatInfo } from "../src/client/js/formatInfo";

describe("Testing the info formatting functionality", () => {
  test("function defined", () => {
    expect(formatInfo).toBeDefined();
  });
});
